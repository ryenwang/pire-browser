# pire-browser platform packages

These package directories define the metadata for the native optional packages used by the
root `pire-browser` npm package. Release automation copies the platform-specific
`pire-browser` and `pire-browser-host` binaries plus the root `README.md` and `LICENSE`
into each package before packing and publishing.
